This is a Support Vector Machine on cell sample dataset. This is also a binary classifier which draws involves separating data into two labels by use of a separator (generally a line, plane or hyperplane). This is one of the best classification models.

